import { SignUpForm } from "@/components/organisms/signUpForm";

export default function SignUp() {
  /*   if (session) {
    return (
      <section>
        <AlreadyLoggedCard />
      </section>
    );
  } */
  return (
    <section>
      <SignUpForm />
    </section>
  );
}
